/* Test models (validations, associations, scopes, cascade on delete, etc.)
 * BlockedTODO: https://github.com/sequelize/sequelize/issues/11408
 * Add model tests using transactional tests when the issue is resolved
 * The only solution right now is truncating or hacking the 'it' function which are both sub-optimal */

test('sample test', () => {
    expect(true).toBe(true);
});
