export const inlineCode = (text) => {
    return '`' + text + '`';
};

export const codeBlock = (text) => {
    return '```\n' + text + '\n```';
};

export const lineBreak = () => {
    return '\n\n<br />\n\n';
};
